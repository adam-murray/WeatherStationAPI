# WeatherStationAPI

## Description
A simple SpringBoot API for fetching weather station data from a SQLiteDatabase and exposing it via GET endpoints.
Currently configured to fetch from a local SQLite file called weather.db at the root of the repository.
